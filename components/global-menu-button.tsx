// Global menu button was used to show the sidebar globally.
// The app now renders a permanent sidebar in `app/layout.tsx`,
// so this component is intentionally left as a placeholder.

export default function GlobalMenuButton() {
  return null;
}
